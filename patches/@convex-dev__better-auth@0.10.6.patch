diff --git a/dist/plugins/convex/index.js b/dist/plugins/convex/index.js
index 030e7f18b940ee5ddecea125800a6c7d6ab52ff3..04fb4d3342443ab18ba59df3c6ab2a9096891ee0 100644
--- a/dist/plugins/convex/index.js
+++ b/dist/plugins/convex/index.js
@@ -32,17 +32,19 @@ const parseAuthConfig = (authConfig, opts) => {
 };
 export const convex = (opts) => {
     const jwtExpirationSeconds = opts.jwt?.expirationSeconds ?? opts.jwtExpirationSeconds ?? 60 * 15;
+    const providerConfig = parseAuthConfig(opts.authConfig, opts);
+    // Use domain from authConfig as issuer (fallback to env var for backwards compat)
+    const issuer = providerConfig.domain || process.env.CONVEX_SITE_URL || "";
     const oidcProvider = oidcProviderPlugin({
         loginPage: "/not-used",
         metadata: {
-            issuer: `${process.env.CONVEX_SITE_URL}`,
-            jwks_uri: `${process.env.CONVEX_SITE_URL}${opts.options?.basePath ?? "/api/auth"}/convex/jwks`,
+            issuer: issuer,
+            jwks_uri: `${issuer}${opts.options?.basePath ?? "/api/auth"}/convex/jwks`,
         },
     });
-    const providerConfig = parseAuthConfig(opts.authConfig, opts);
     const jwtOptions = {
         jwt: {
-            issuer: `${process.env.CONVEX_SITE_URL}`,
+            issuer: issuer,
             audience: "convex",
             expirationTime: `${jwtExpirationSeconds}s`,
             definePayload: ({ user, session }) => ({
